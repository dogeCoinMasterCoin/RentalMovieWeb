@model Data1.Models.UserWatchList

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>UserWatchList</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="UserWatchListId" />
            <div class="form-group">
                <label asp-for="WatchListId" class="control-label"></label>
                <select asp-for="WatchListId" class="form-control" asp-items="ViewBag.WatchListId"></select>
                <span asp-validation-for="WatchListId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserAppId" class="control-label"></label>
                <select asp-for="UserAppId" class="form-control" asp-items="ViewBag.UserAppId"></select>
                <span asp-validation-for="UserAppId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
