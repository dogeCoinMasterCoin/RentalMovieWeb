@model IEnumerable<Data1.Models.UserWatchList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WatchList)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserApp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WatchList.WatchListId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserApp.UserAppId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserWatchListId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserWatchListId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserWatchListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
